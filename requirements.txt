# ─── Core ───
python-dotenv==1.0.1
psycopg2-binary==2.9.9
sqlalchemy==2.0.36
pandas==2.2.3

# ─── LLM providers ───
# Gemini + OpenAI + DeepSeek + Groq are all reached via the OpenAI-compatible
# client (single dependency). Ollama uses plain HTTP via requests.
# Claude uses its own SDK (not OpenAI-compatible).
openai==1.59.2
anthropic==0.69.0
requests==2.32.3

# ─── Streaming + data lake ───
kafka-python-ng==2.2.3  # Kafka producer/consumer (py3.12-compatible fork)
boto3==1.35.90          # S3 client for MinIO (raw data lake)

# ─── SQL safety / parsing ───
sqlparse==0.5.3

# ─── UI ───
streamlit==1.41.1
altair==5.5.0
streamlit-autorefresh==1.0.1   # realtime dashboard auto-refresh
